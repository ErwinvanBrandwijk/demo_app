<%= form_for(@wishlistline) do |f| %>
  <% if @wishlistline.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@wishlistline.errors.count, "error") %> prohibited this wishlistline from being saved:</h2>

      <ul>
      <% @wishlistline.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :quantity %><br />
    <%= f.number_field :quantity %>
  </div>
  <div class="field">
    <%= f.label :price %><br />
    <%= f.text_field :price %>
  </div>
  <div class="field">
    <%= f.label :bought %><br />
    <%= f.number_field :bought %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
